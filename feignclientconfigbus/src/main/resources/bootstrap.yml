spring:
  cloud:
    config:
      uri: http://localhost:8088
      profile: dev
      label: master
  application:
    name: foobar
  rabbitmq:
      host: localhost
      port: 5672
      username: guest
      password: guest